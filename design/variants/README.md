# Route B variants (home page, es)

- **a.html, "Court from above":** framed photo off-centre with the headline overlapping its edge; a full-width court outline draws itself behind the hero; programmes as a 2x2 court diagram; Metodología as a horizontal 4-step drill row under a photo band; prices as an Ink scoreboard; reviews as one large quote plus two small; booking on clay.
- **b.html, "Court as page grid":** full-bleed photo with a clay panel rising from the bottom-left; four court lines run the whole page as its columns and draw down on load; Metodología first as a sticky heading with a vertical drill list; one large full-width review on clay; programmes and prices merged into ruled rows plus a four-cell price strip; booking on sand with a form.

Both: DESIGN.md palette and Archivo only, uppercase expanded display, Ball yellow on the primary CTA only, court-line draw + one ball bounce, reduced-motion fallback = lines drawn, ball resting.
Team section omitted: the current coach list is unconfirmed (brief GAPS), so DESIGN.md's team scoreboard is replaced by the price scoreboard until the client answers.
